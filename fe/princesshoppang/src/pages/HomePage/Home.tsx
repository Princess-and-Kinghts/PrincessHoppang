import Button from "../../components/Button";

const Home = () => {
  return (
    <div>
      <h1>Home</h1>
      <Button shapeType="confirm">확인하기</Button>
      <Button shapeType="pill">ENTP</Button>
    </div>
  );
};

export default Home;
